# Public build-time variable; supply in shell/Vercel (not automatically loaded from this file).
# Locally /api is proxied by ng serve. On Vercel use the HTTPS backend URL.
API_URL=https://your-api.onrender.com
